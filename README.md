## Features
- Real-time monitoring of CPU, memory, temperature, and network latency across multiple servers
- ML-based anomaly detection using Isolation Forest, with adaptive retraining on live data
- Interactive dashboard with live-updating charts (Chart.js)
- Multi-server support with tab-based switching
- Anomaly alert log showing recent detected issues with confidence scores
